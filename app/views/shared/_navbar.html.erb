<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pictory</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to("Tus historias", history_list_path) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.try(:admin?) %>
          <li><%= link_to("Administración", users_path) %></li>
        <% end %>
        <li class="dropdown">
          <% if user_signed_in? %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> <%= current_user.username %> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to("Editar cuenta", edit_user_registration_path) %></li>
            <li><%= link_to("Salir", destroy_user_session_path , method: :delete) %></li>
          </ul>
          <% else %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> Acceso <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Entrar a tu cuenta", new_user_session_path %></li>
            <li><%= link_to "Crear una cuenta", new_user_registration_path %></li>
          </ul>
        <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>

